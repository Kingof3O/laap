{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the signed LAAP desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
