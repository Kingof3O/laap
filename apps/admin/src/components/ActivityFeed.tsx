import { Activity, ArrowUpRight, CircleDot, Clock3, ShieldCheck } from 'lucide-react'
import type { ActivityItem } from '../lib/data'
import { GlassCard } from './GlassCard'

export function ActivityFeed({ items }: { items: ActivityItem[] }) {
  return <GlassCard className="p-5 sm:p-6"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><span className="section-icon section-icon-cyan"><Activity aria-hidden="true" size={16} /></span><h2 className="section-title">Recent activity</h2></div><p className="mt-2 text-xs text-slate-500">Latest changes in your workspace</p></div><button type="button" className="icon-button h-9 w-9" aria-label="Open full activity log"><ArrowUpRight aria-hidden="true" size={16} /></button></div><ol className="mt-6 space-y-5">{items.map((item, index) => <li key={item.id} className="relative flex gap-3">{index < items.length - 1 ? <span className="absolute left-[7px] top-5 h-[calc(100%+12px)] w-px bg-white/[0.07]" aria-hidden="true" /> : null}<span className={`activity-dot activity-dot-${item.tone}`} aria-hidden="true">{item.tone === 'success' ? <ShieldCheck size={11} /> : item.tone === 'warning' ? <Clock3 size={11} /> : <CircleDot size={11} />}</span><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><p className="text-xs font-medium text-slate-300">{item.title}</p><time className="shrink-0 font-mono text-[10px] text-slate-600">{item.time}</time></div><p className="mt-1 truncate text-[11px] text-slate-500">{item.detail}</p></div></li>)}</ol><button type="button" className="mt-6 inline-flex min-h-11 cursor-pointer items-center gap-2 text-xs font-medium text-slate-400 transition hover:text-slate-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300/80">Open audit log <ArrowUpRight aria-hidden="true" size={14} /></button></GlassCard>
}
