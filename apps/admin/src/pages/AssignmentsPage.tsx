import { useEffect, useState, type FormEvent, type ReactNode } from 'react'
import { ArrowRight, Link2, UserPlus, X } from 'lucide-react'
import type { ApiUser, DashboardAccount } from '@laap/types'
import { api, ApiError } from '../lib/api'
import { GlassCard } from '../components/GlassCard'
import { StatusBadge } from '../components/StatusBadge'

type Assignment = { id: string; accountId: string; userId: string; account: string; user: string; email: string; status: string; assignedAt: string; expiresAt: string | null }
type AssignmentsPageProps = { initialAccounts: DashboardAccount[]; offline: boolean; onToast: (message: string) => void }

export function AssignmentsPage({ initialAccounts, offline, onToast }: AssignmentsPageProps) {
  const [rows, setRows] = useState<Assignment[]>([])
  const [accounts, setAccounts] = useState(initialAccounts)
  const [users, setUsers] = useState<ApiUser[]>([])
  const [form, setForm] = useState({ accountId: initialAccounts[0]?.id ?? '', userId: '', expiresAt: '' })
  const [saving, setSaving] = useState(false)
  const refresh = async () => { if (offline) return; try { const [assignmentResult, accountResult, userResult] = await Promise.all([api.getAssignments(), api.getAccounts(), api.getUsers()]); setRows(assignmentResult.assignments); setAccounts(accountResult.accounts); setUsers(userResult.users.filter((user) => user.role !== 'admin')) } catch (error) { onToast(error instanceof ApiError ? error.message : 'Unable to load assignments') } }
  useEffect(() => { void refresh() }, [offline])
  const assign = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setSaving(true); try { await api.addAssignment({ accountId: form.accountId, userId: form.userId, expiresAt: form.expiresAt ? new Date(form.expiresAt).toISOString() : null }); onToast('Assignment saved'); setForm({ ...form, expiresAt: '' }); await refresh() } catch (error) { onToast(error instanceof ApiError ? error.message : 'Unable to save assignment') } finally { setSaving(false) } }
  const revoke = async (row: Assignment) => { try { await api.revokeAssignment(row.accountId, row.userId); onToast(`Access revoked for ${row.user}`); await refresh() } catch (error) { onToast(error instanceof ApiError ? error.message : 'Unable to revoke assignment') } }
  return <div className="mx-auto max-w-[1600px] px-5 py-7 sm:px-8 sm:py-9 lg:px-10 lg:py-10"><p className="eyebrow">Access / Assignments</p><h1 className="display-title mt-3">Access with guardrails.</h1><p className="mt-4 max-w-xl text-sm leading-6 text-slate-400">Give operators the minimum access they need. Every assignment is time-bounded and auditable.</p><GlassCard className="mt-7 p-5 sm:p-6"><div className="flex items-center gap-2"><span className="section-icon section-icon-cyan"><UserPlus aria-hidden="true" size={16} /></span><div><h2 className="section-title">Create assignment</h2><p className="mt-1 text-xs text-slate-500">Choose a pooled account and an authorized operator.</p></div></div><form className="mt-5 grid gap-4 md:grid-cols-[1.2fr_1.2fr_1fr_auto] md:items-end" onSubmit={assign}><Field label="Account"><select className="input-base h-11 w-full cursor-pointer px-3 text-sm" value={form.accountId} onChange={(event) => setForm({ ...form, accountId: event.target.value })} required>{accounts.filter((account) => account.status !== 'Maintenance').map((account) => <option key={account.id} value={account.id}>{account.name} · {account.region}</option>)}</select></Field><Field label="Operator"><select className="input-base h-11 w-full cursor-pointer px-3 text-sm" value={form.userId} onChange={(event) => setForm({ ...form, userId: event.target.value })} required><option value="">Select operator</option>{users.map((user) => <option key={user.id} value={user.id}>{user.displayName} · {user.email}</option>)}</select></Field><Field label="Expires (optional)"><input className="input-base h-11 w-full px-3 text-sm" type="datetime-local" value={form.expiresAt} onChange={(event) => setForm({ ...form, expiresAt: event.target.value })} /></Field><button type="submit" disabled={saving || offline} className="inline-flex min-h-11 cursor-pointer items-center justify-center gap-2 rounded-xl border border-cyan-300/20 bg-cyan-300/10 px-4 text-xs font-medium text-cyan-100 transition hover:bg-cyan-300/15 disabled:cursor-not-allowed disabled:opacity-50">{saving ? 'Saving…' : offline ? 'API offline' : 'Grant access'}<ArrowRight aria-hidden="true" size={14} /></button></form></GlassCard><GlassCard className="mt-5 overflow-hidden"><div className="flex items-center gap-2 border-b border-white/[0.06] px-5 py-5 sm:px-6"><span className="section-icon section-icon-violet"><Link2 aria-hidden="true" size={16} /></span><div><h2 className="section-title">Active assignments <span className="font-mono text-[11px] text-slate-600">{rows.filter((row) => row.status === 'active').length}</span></h2><p className="mt-1 text-xs text-slate-500">Who can claim which account</p></div></div><div className="divide-y divide-white/[0.05]">{rows.map((row) => <div key={row.id} className="flex flex-wrap items-center gap-4 px-5 py-4 sm:px-6"><div className="account-glyph account-glyph-violet" aria-hidden="true">{row.account.slice(0, 1)}</div><div className="min-w-[170px] flex-1"><p className="font-mono text-xs font-medium text-slate-200">{row.account}</p><p className="mt-1 text-[11px] text-slate-500">{row.user} · {row.email}</p></div><StatusBadge value={row.status === 'active' ? 'Available' : 'Maintenance'} compact /><button type="button" onClick={() => void revoke(row)} disabled={row.status !== 'active' || offline} className="inline-flex min-h-11 cursor-pointer items-center gap-1.5 rounded-lg border border-transparent px-3 text-[11px] text-slate-500 transition hover:border-rose-400/20 hover:bg-rose-400/10 hover:text-rose-300 disabled:cursor-not-allowed disabled:opacity-40 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300/80"><X aria-hidden="true" size={13} />Revoke</button></div>)}{!rows.length ? <p className="px-6 py-10 text-center text-xs text-slate-500">Assignment data becomes available after the API connects.</p> : null}</div></GlassCard></div>
}

function Field({ label, children }: { label: string; children: ReactNode }) { return <label className="block"><span className="mb-2 block text-[11px] font-medium uppercase tracking-[0.12em] text-slate-500">{label}</span>{children}</label> }
